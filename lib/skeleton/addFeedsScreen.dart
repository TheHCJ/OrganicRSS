import 'package:flutter/material.dart';

class addFeedsScreen extends StatefulWidget {
  State<addFeedsScreen> createState() => addFeedsScreenState();
}

class addFeedsScreenState extends State<addFeedsScreen> {
  Widget build(BuildContext context) {
    return Scaffold(
        appBar: AppBar(title: Text("Add feeds"),),
      );
  }
}